rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // Allow read and write access if the user is authenticated (request.auth is not null)
      allow read, write: if request.auth != null;
    }
  }
}