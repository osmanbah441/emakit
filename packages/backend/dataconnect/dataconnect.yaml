specVersion: "v1"
serviceId: "e-makit-service"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "e-makit-database"
      cloudSql:
        instanceId: "e-makit-instance"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
